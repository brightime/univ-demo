# ============================================================
# 必要なライブラリのインストール:
#   pip install streamlit google-genai
#
# 起動方法:
#   streamlit run app.py
#
# 事前準備:
#   .streamlit/secrets.toml に以下を記載
#   GEMINI_API_KEY = "your-api-key-here"
# ============================================================

import json
import streamlit as st
from google import genai
from google.genai import types

# ──────────────────────────────────────────
# 埋め込みシラバスデータ
# ──────────────────────────────────────────
syllabus_data = [
    {
        "course_name": "イノベーション・マネジメント実践演習",
        "teacher": "久保田達也 他",
        "target_student": "社会を変えたい人、起業に興味がある人、AIビジネスを知りたい人",
        "content_summary": "AIなどの最先端技術を社会課題にどう応用するか、実務家と議論する演習。グループワーク多め。",
        "hard_truth": "英語での発表があるかもしれないから覚悟が必要。",
        "materials": [
            "『FACTFULNESS』(ハンス・ロスリング)",
            "『ゼロからつくるビジネスモデル』(井上達彦)",
            "『自分の頭で考える日本の論点』(出口治明)"
        ]
    },
    {
        "course_name": "経済政策",
        "teacher": "福島章雄",
        "content_summary": "戦後の日本経済の発展と、今の少子高齢化などの課題を、理論と歴史から読み解く。",
        "target_student": "ニュースを深く理解したい人、公務員や金融業界を目指す人",
        "hard_truth": "期末試験のウェイトが高い(70%)。真面目にノートを取らないと単位は来ない。",
        "materials": [
            "『はじめての経済学 上・下』(伊藤元重 / 日本経済新聞出版)"
        ]
    },
    {
        "course_name": "アート、デザインとクリエイティビティ",
        "teacher": "積田淳史",
        "content_summary": "アートやデザインがビジネス・イノベーションにどう貢献するかを学ぶ。感性を磨く授業。",
        "target_student": "広告・企画職に興味がある人、クリエイティブな発想が欲しい人",
        "hard_truth": "【要注意】美術館見学で4〜5千円の自腹が発生する。ChatGPTの使用は禁止！スマホ必須。",
        "materials": [
            "映画『天才たちの頭の中 世界を面白くする107のヒント』",
            "映画『マーク・ジェイコブズ＆ルイ･ヴィトン』",
            "映画『TOVE／トーベ』"
        ]
    }
]

# ──────────────────────────────────────────
# システムプロンプト
# ──────────────────────────────────────────
SYSTEM_PROMPT_TEMPLATE = """成城大学入学検討中の高校生に授業を紹介するメンターAI。
一人称「私」、丁寧で知的な口調、絵文字は要所のみ。楽単情報は禁止。

【シラバス】{syllabus_context}

【回答順序】①悩みへの共感(1-2文) ②授業名+教員名+推薦理由 ③各教材(難易度★/成長/一言) ④hard_truthを必ず明記 ⑤締めくくり
【禁止】楽単情報/架空授業/hard_truth省略/成城大以外の授業紹介"""

def build_system_prompt() -> str:
    context = json.dumps(syllabus_data, ensure_ascii=False, indent=2)
    return SYSTEM_PROMPT_TEMPLATE.format(syllabus_context=context)


# ──────────────────────────────────────────
# ウェルカムメッセージ
# ──────────────────────────────────────────
WELCOME_MESSAGE = """\
こんにちは。成城大学 授業案内メンターAIです。

成城大学への入学を検討されている方に向けて、授業の内容や学びの魅力を
できるだけ正直にお伝えします。

**こんな相談が得意です**

- 「将来、起業やビジネスに関わる仕事をしたい」
- 「経済やニュースの背景をきちんと理解したい」
- 「広告・デザイン・クリエイティブな仕事に興味がある」
- 「社会問題に向き合う仕事がしたい」

授業選びは、大学生活の質を大きく左右します。
「なんとなく」ではなく、あなたの目標から逆算して一緒に考えましょう。

どんな悩みや興味でも、気軽に話しかけてください。\
"""

# ──────────────────────────────────────────
# Streamlit ページ設定
# ──────────────────────────────────────────
st.set_page_config(
    page_title="成城大学 授業案内AI",
    page_icon="🎓",
    layout="centered",
    initial_sidebar_state="collapsed",
)

# スマホ最適化 CSS
st.markdown(
    """
    <style>
        .block-container {
            max-width: 720px;
            padding: 3.5rem 1rem 5rem 1rem;
        }
        /* タイトルを2行で収まるよう調整 */
        h1 {
            font-size: 1.25rem !important;
            line-height: 1.4 !important;
            word-break: keep-all !important;
        }
        .stChatMessage { border-radius: 12px; }
        .stChatInputContainer { padding-bottom: 0.5rem; }
    </style>
    """,
    unsafe_allow_html=True,
)

# ──────────────────────────────────────────
# ヘッダー
# ──────────────────────────────────────────
st.markdown("# 🎓 成城大学 授業案内メンターAI")
st.caption("高校生のための授業ガイド｜あなたの興味・将来の目標から、学ぶべき授業を一緒に考えます")
st.divider()

# ──────────────────────────────────────────
# Gemini API の初期化（新SDK: google-genai）
# ──────────────────────────────────────────
if "gemini_client" not in st.session_state:
    try:
        api_key = st.secrets["GEMINI_API_KEY"]
    except Exception:
        st.error("⚠️ APIキーが見つかりません。Streamlit Cloud の Secrets に GEMINI_API_KEY を設定してください。")
        st.stop()

    try:
        st.session_state.gemini_client = genai.Client(api_key=api_key)
        st.session_state.chat_config = types.GenerateContentConfig(
            system_instruction=build_system_prompt(),
            temperature=0.7,
            top_p=0.95,
            max_output_tokens=2048,
        )
    except Exception as e:
        st.error(f"⚠️ AIの初期化に失敗しました。\n\n```\n{e}\n```")
        st.stop()

# ──────────────────────────────────────────
# セッションステートの初期化
# ──────────────────────────────────────────
if "messages" not in st.session_state:
    st.session_state.messages = []

if "gemini_chat" not in st.session_state:
    st.session_state.gemini_chat = st.session_state.gemini_client.chats.create(
        model="gemini-2.5-flash-lite",
        config=st.session_state.chat_config,
    )

# ──────────────────────────────────────────
# チャット履歴の描画
# ──────────────────────────────────────────
if not st.session_state.messages:
    with st.chat_message("assistant", avatar="🎓"):
        st.markdown(WELCOME_MESSAGE)

for msg in st.session_state.messages:
    avatar = "🎓" if msg["role"] == "assistant" else "🙋"
    with st.chat_message(msg["role"], avatar=avatar):
        st.markdown(msg["content"])

# ──────────────────────────────────────────
# ユーザー入力 & AI 応答（ストリーミング）
# ──────────────────────────────────────────
if user_input := st.chat_input("将来の夢や興味を教えてください（例：起業に興味がある、経済を学びたい）"):

    # ユーザーメッセージを表示・保存
    st.session_state.messages.append({"role": "user", "content": user_input})
    with st.chat_message("user", avatar="🙋"):
        st.markdown(user_input)

    # Gemini へストリーミングリクエスト & 逐次表示
    with st.chat_message("assistant", avatar="🎓"):
        try:
            # 会話履歴が長くなりすぎたら新しいチャットセッションに切り替え（直近5ターン分を保持）
            MAX_TURNS = 5
            if len(st.session_state.messages) > MAX_TURNS * 2:
                from google.genai import types as _types
                history = []
                for msg in st.session_state.messages[-(MAX_TURNS * 2):]:
                    role = "user" if msg["role"] == "user" else "model"
                    history.append(_types.Content(role=role, parts=[_types.Part(text=msg["content"])]))
                st.session_state.gemini_chat = st.session_state.gemini_client.chats.create(
                    model="gemini-2.5-flash-lite",
                    config=st.session_state.chat_config,
                    history=history,
                )

            def chunk_generator():
                for chunk in st.session_state.gemini_chat.send_message_stream(user_input):
                    if chunk.text:
                        yield chunk.text

            full_response = st.write_stream(chunk_generator())

        except Exception as e:
            full_response = f"エラーが発生しました。\n\n```\n{e}\n```"
            st.error(full_response)

    st.session_state.messages.append({"role": "assistant", "content": full_response})

# ──────────────────────────────────────────
# サイドバー
# ──────────────────────────────────────────
with st.sidebar:
    st.markdown("## 相談例")
    st.markdown(
        """
        - 「AIやテクノロジーで社会課題を解決したい」
        - 「経済の仕組みをゼロから理解したい」
        - 「広告やブランディングに関わる仕事がしたい」
        - 「公務員・金融業界を目指している」
        - 「デザインやアートで表現する仕事に興味がある」

        ---

        **成城大学について**

        東京・成城に位置する少人数制の私立大学。
        経済学部・文芸学部・法学部・社会イノベーション学部の4学部を擁し、
        きめ細やかな教育環境が特徴です。

        ---

        **このツールについて**

        授業シラバスをもとに、AIが参考文献の解説や
        履修上の注意点まで誠実にお伝えします。
        楽単情報はお教えしていません。
        """
    )

    st.divider()
    if st.button("会話をリセットする", use_container_width=True):
        st.session_state.messages = []
        st.session_state.gemini_chat = st.session_state.gemini_client.chats.create(
            model="gemini-2.5-flash-lite",
            config=st.session_state.chat_config,
        )
        st.rerun()
